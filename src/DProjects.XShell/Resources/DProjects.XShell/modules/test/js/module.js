export default class {

    // ctor
    constructor({ bus, config, params }) {
        this._bus = bus;
        this._config = config;
        this._params = params;
    }

    // methods
    async start() {
    }
    async stop() {
    }
}